# Brainlume GitHub Pages 上传说明

1. 解压 `Brainlume_APP_1.0_GitHub_Pages_Upload.zip`。
2. 在 GitHub 仓库 `brdoyhu/brainlumeapp1.0` 中选择 **Add file → Upload files**。
3. 上传解压后的全部文件；不要只上传 ZIP，也不要再套一层文件夹。
4. 提交后进入 **Settings → Pages**。
5. 在 **Build and deployment** 中选择 **Deploy from a branch**。
6. Branch 选择 `main`，Folder 选择 `/ (root)`，点击 **Save**。

发布地址预计为：

`https://brdoyhu.github.io/brainlumeapp1.0/`

注意：互动版中的脑电与结果数据为内部体验模拟数据，不代表真实医疗检测结果。
